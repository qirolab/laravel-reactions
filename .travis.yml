language: php

php:
  - 7.1
  - 7.2
  - 7.3
  - hhvm

env:
  matrix:
    - COMPOSER_FLAGS="--prefer-lowest --no-interaction"
    - COMPOSER_FLAGS=""

script:
  - vendor/bin/phpunit --coverage-text --coverage-clover=coverage.clover

after_script:
  - php vendor/bin/ocular code-coverage:upload --format=php-clover coverage.clover